# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
# dist/
build/
eggs/
parts/
var/
sdist/
develop-eggs/
.installed.cfg
lib/
lib64/

# Virtual environments
.env
.venv
myvenv/
env/
venv/
ENV/

# Environment variables — NEVER commit these
.env
.env.local
.env.*.local
.env.production

# NiceGUI
.nicegui/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
logs/

# Railway
.railway/

# Cache files — dashboard_cache.json is intentionally committed
# as static demo data, but exclude any future runtime caches
*_cache_runtime.json

# Large static GTFS artifact; database bootstrap downloads the current archive
data/stop_times.txt

# JavaScript dependencies. frontend/dist remains tracked until Railway has a
# verified frontend build step.
node_modules/

# Local and remote environment profiles
.env.remote

# Temporary user-provided notes may contain personal or deployment details
docs/temp_doc/

# Historical chat is retained locally and may contain copied credentials
docs/CHAT_HISTORY_RAW.md
